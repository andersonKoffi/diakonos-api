services:
  postgres:
    image: postgres:16-alpine
    container_name: diakonos-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-diakonos}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-diakonos}
      POSTGRES_DB: ${POSTGRES_DB:-diakonos}
    ports:
      - '${POSTGRES_PORT:-5432}:5432'
    volumes:
      - diakonos_pgdata:/var/lib/postgresql/data
    healthcheck:
      test:
        [
          'CMD-SHELL',
          'pg_isready -U ${POSTGRES_USER:-diakonos} -d ${POSTGRES_DB:-diakonos}',
        ]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  diakonos_pgdata:
